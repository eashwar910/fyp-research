# RUN_LOG — run <N>

Copy to `.research/RUN_LOG.md` at stage 0. Fill each block when the stage
exits. Keep it terse; this is for resuming and auditing, not narrative.

## Stage 0 — preflight
- date:
- constraints.yml version / sha:
- buckets.yml sha:
- archive runs indexed: [run3, ...] → N prior candidates
- open TODOs at start: (must be none)

## Stage 1 — landscape
- dispatches: 3 (india, singapore_malaysia, eu)
- searches used / budget:
- crowded entries (merged):
- forcing functions (merged, dated):
- re-dispatches and why:

## Stage 2 — gap hunt
- dispatch matrix:
- one-liners returned per hunter:
- after dedupe:
- data_class distribution (this is the "did hunters drift to optical NDVI" check):
- archive restatements flagged:

## Stage 3 — SCREEN
- assessed / passed / kill rate:
- kills by gate: C3 __ / C1 __ / excl __ / C7 __
- borderline calls (id + one line each):

## Stage 4 — verify
- survivors: N → adversary dispatches N, verifier dispatches N
- novelty: closed __ / type_a __ / type_b __ / unclear __
- data: open __ / conditional __ / blocked __
- second-pass adversary dispatches:
- URL spot-check: sampled __ / 404s __ / re-verified __

## Stage 5 — elaborate
- dispatches:
- `needs:` returned (id → what → routed to):

## Stage 6 — FULL
- PASS / CONDITIONAL / FAIL:
- region mix (observation):
- scope-floor distribution:

## Stage 7 — roster
- roster length:
- top-3 by ranking key:
- archived to: research-archive/run<N>/

## Budget
- total tool calls (searches + fetches):
- over/under ceiling and where:

## Open questions for the owner
- (things only a human can resolve — a partner dataset, a licence email, an application deadline)
